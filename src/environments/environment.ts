// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCwAfWojsZgq1xN48pJ5d2hpR6xuyFqsYc",
    authDomain: "dresen-codes.firebaseapp.com",
    databaseURL: "https://dresen-codes.firebaseio.com",
    projectId: "dresen-codes",
    storageBucket: "dresen-codes.appspot.com",
    messagingSenderId: "29716759684",
    appId: "1:29716759684:web:ad34c9fbd320d6ef"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
